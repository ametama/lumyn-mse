{
  "name": "lumyn-mse-client",
  "version": "1.0.0",
  "main": "build/main.js",
  "scripts": {
    "build": "npx tsc --build; npx esbuild build/main.js --bundle --minify --outfile=dist/client.min.js; npx esbuild src/components/App.css --minify --bundle --outfile=dist/style.min.css"
  },
  "author": "ametama",
  "license": "UNLICENSED",
  "description": "",
  "devDependencies": {
    "@eslint/js": "^9.16.0",
    "esbuild": "^0.24.0",
    "eslint": "^9.16.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0"
  }
}
